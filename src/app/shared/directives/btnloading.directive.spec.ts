import { BtnloadingDirective } from './btnloading.directive';

describe('BtnloadingDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnloadingDirective();
    expect(directive).toBeTruthy();
  });
});
